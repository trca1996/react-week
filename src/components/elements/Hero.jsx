import React from 'react';
import PropTypes from 'prop-types';
import Heading from './Heading.jsx';

const Hero = props => (
  <div>
    <Heading variant={h1} title={''} />
  </div>
);

Hero.propTypes = {};

export default Hero;
